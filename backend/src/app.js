/* Importa os arquivos necessários para a aplicação principal do sistema */
const express = require("express");
const routes = require("./routes");
const { errors } = require("celebrate");
const cors = require("cors");

module.exports = app;
